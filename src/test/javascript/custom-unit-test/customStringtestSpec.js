describe('Grocery list checklist', ()=>{
    it('should add an item');
        var  todo = new Grocerylist();
        var item = {
            title: "get eggs",
            complete: false
        }
         done = todo.addGroceryList(item)
        expect(todo.getItems().length).toBe(1);
    });
})


var calculator = function () {
    return {
        add: function(x, y){
            return a
        };
        subtract: (z, w);


    }

}